# MySQL Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=quizmaster_db

# Next.js Secrets (Optional for now as we use simple bcrypt)
# NEXTAUTH_SECRET=your_secret_here
